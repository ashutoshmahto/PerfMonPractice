/**
 * Include webpack configuration for future app develoment for test validation of lighthouse extension
 */
const path = require('path');
const webpack = require('webpack');

module.exports = {
    entry: {},
    output: {},
    module: {
        loaders:[{}]
    }
}